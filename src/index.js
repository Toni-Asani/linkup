import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div style={{background:'red', color:'white', fontSize:40, padding:50}}>
    HUBBING FONCTIONNE
  </div>
);